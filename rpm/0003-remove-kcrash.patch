diff --git a/src/CMakeLists.txt b/src/CMakeLists.txt
index 0508028..a63a6aa 100644
--- a/src/CMakeLists.txt
+++ b/src/CMakeLists.txt
@@ -65,8 +65,6 @@ if (ANDROID)
     )
 
     ecm_add_android_apk(ktrip ANDROID_DIR ${CMAKE_SOURCE_DIR}/android)
-else()
-    target_link_libraries(ktrip PRIVATE Qt::Widgets KF6::Crash)
 endif()
 
 ecm_add_app_icon(ktrip ICONS 256-org.kde.ktrip.png 150-org.kde.ktrip.png)
diff --git a/src/main.cpp b/src/main.cpp
index e6111e8..23a947c 100644
--- a/src/main.cpp
+++ b/src/main.cpp
@@ -18,7 +18,6 @@
 #ifdef Q_OS_ANDROID
 #include <QGuiApplication>
 #else
-#include <KCrash>
 #include <QApplication>
 #endif
 
@@ -77,10 +76,6 @@ int main(int argc, char *argv[])
     about.setBugAddress("https://invent.kde.org/utilities/ktrip/-/issues");
     KAboutData::setApplicationData(about);
 
-#ifndef Q_OS_ANDROID
-    KCrash::initialize();
-#endif
-
 #ifdef Q_OS_ANDROID
     engine.rootContext()->setContextProperty(QStringLiteral("_isAndroid"), true);
     engine.rootContext()->setContextProperty(QStringLiteral("_androidUtils"), QVariant::fromValue(AndroidUtils::instance()));
